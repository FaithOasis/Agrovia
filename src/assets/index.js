// Re-export assets located in /public so components can import them like module assets.
// This avoids needing to copy binary files into src/ while keeping import semantics.
export const logo = "/logo-dark-transparent.png";

// Example usage:
// import { logo } from "../assets";
// <img src={logo} />
